import { CreateUserValidatePipe } from './create-user-validate.pipe';

describe('CreateUserValidatePipe', () => {
  it('should be defined', () => {
    expect(new CreateUserValidatePipe()).toBeDefined();
  });
});
